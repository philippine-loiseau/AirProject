import { DynamicDatatableDirective } from './dynamic-datatable.directive';

describe('DynamicDatatableDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDatatableDirective();
    expect(directive).toBeTruthy();
  });
});
