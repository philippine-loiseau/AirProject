import { ToggleColumnsPipe } from './toggle-columns.pipe';

describe('ToggleColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
